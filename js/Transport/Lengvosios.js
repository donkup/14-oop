const Transport = require ('./Transport');

class Lengvosios extends Transport{
    constructor (name, sterWhell, seatCount, wheelCount,){
        super (name, sterWhell, seatCount, wheelCount,);

    }
}

module.exports = Lengvosios;